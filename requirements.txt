# BIA-5401 Final Project — dependencies
# Python 3.13.1
#
# The project runs across two environments:
#   - Local  : data_cleaning.ipynb, ml_model_lr_0809.ipynb
#   - Colab  : dl_model_distilbert.ipynb (GPU runtime required)
#
# Installing this file covers the local notebooks. The Colab notebook installs
# its own deep-learning dependencies in Step 0 — see the bottom of this file.

# ---------------------------------------------------------------------------
# Local environment — install these
# ---------------------------------------------------------------------------

# core data handling
pandas==2.2.2
pyarrow>=15.0            # parquet read/write backend for pandas
duckdb>=1.0              # out-of-core filtering of the 9 GB source CSV

# machine learning
scikit-learn==1.6.1
joblib>=1.3              # persisting the fitted model and vectorizer

# plotting
matplotlib>=3.8

# notebook environment
jupyter>=1.0
ipykernel>=6.0

# ---------------------------------------------------------------------------
# Colab environment — do NOT install locally
# ---------------------------------------------------------------------------
# The DistilBERT notebook runs on Google Colab with a T4 GPU. Colab already
# provides torch, pandas, numpy, scikit-learn and matplotlib. Only the three
# packages below need installing, and Step 0 of that notebook does it:
#
#     !pip install -q transformers datasets accelerate
#
# Left commented out deliberately: torch is ~2-3 GB and is not needed by either
# local notebook. Uncomment only if you intend to fine-tune DistilBERT locally
# on your own GPU instead of using Colab.
#
# torch>=2.0             # PyTorch — the framework Transformers runs on
# transformers>=4.40     # DistilBERT model, tokenizer, and Trainer API
# datasets>=2.19         # Dataset.from_pandas + batched tokenization
# accelerate>=0.30       # required backend for Trainer on GPU
